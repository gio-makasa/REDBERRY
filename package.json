{
  "name": "notes-app",
  "version": "0.0.0",
  "scripts": {
    "dev": "set NODE_OPTIONS=--openssl-legacy-provider && vue-cli-service vite",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && vue-cli-service vite build",
    "preview": "set NODE_OPTIONS=--openssl-legacy-provider && vue-cli-service vite preview"
  },
  "dependencies": {
    "vue": "^3.2.41",
    "vue-router": "^4.0.13",
    "vuex": "^4.0.2"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.1.2",
    "vite": "^3.1.8"
  }
}
